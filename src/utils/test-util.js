import { translateFromContent } from "./translateFromContent.js";

const string = "content";

const input = { "C": "default", cs: "zaklad", en: "default", DE: "voreinstellung" };

const inputUndefined = undefined;
const emptyObj = {};
const emptyStr = "";


// console.log(`output: ${translateFromContent(input)}`);